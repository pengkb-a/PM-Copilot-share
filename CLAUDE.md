# PM Copilot（产品经理 AI 助手）

> ⚠️ **本工作流处于「未定制」状态。** 首次使用前（无论你是人，还是 AI 助手），必须先阅读 `docs/改造指南.md` 并完成其中的定制清单。任何 `{{...}}` 占位符、`> ⚠️ 待填写` 标记都表示尚未定制的部分，请勿在未定制状态下用默认值直接运行。

本工作区是产品经理 AI 工作流，覆盖从原始需求到 PRD 撰写、原型生成、飞书发布的全链路。所有能力以 **Skills**（`.claude/skills/`）形式组织，按需加载，可在 Claude Code、Codex、Kiro、DeepSeek harness 等 agent 工具中运行。

---

## 项目背景（待填写）

> ⚠️ 待填写：下面是你业务系统的背景，请按 `config/project-context-template.md` 填充并替换本段。

{{PRODUCT_NAME}} 是 {{PROJECT_NAME}} 的核心系统，服务于（一句话：是什么、服务谁、覆盖哪些业务线）。核心职责：（列出 2-3 条）。

> 业务细节（术语、账户、规则等）见 `knowledge/` 下你挂载的知识包，需要时加载。

### 当前主要需求方向

- {{需求方向 1}}
- {{需求方向 2}}

---

## 全局约定

### 交互语言

全程使用中文，包括对话回复、选择题的问题文本/选项、需求与文档正文。代码标识符、API 字段名、工具名、错误码保持英文原样。

### 文档输出规范（飞书兼容）

- **标题层级**：`#` 文档大标题 → `##` 一级章节 → `###` 二级 → `####` 三级 → `#####` 四级 → `######` 五级 → 六级以下用加粗编号
- **流程图必须用 Mermaid**：多系统交互 `flowchart TD` | 接口时序 `sequenceDiagram` | 状态流转 `stateDiagram-v2`
- **格式禁忌**：禁止 HTML 标签 | 重要提示用 `>` 或 💡 | 表格用标准 Markdown

### PRD 结构约定

PRD 遵循统一模板，详见 `prd-template` 技能。

### 输出文件规则

| 输出类型 | 存放位置 |
|---------|---------|
| PRD 文档 | 生成前确认输出形式：本地 `PRD文档/PRD-[功能名称].md` 或 飞书云文档 |
| HTML 原型 | `原型文件/[PRD名称]/` |
| drawio 图 | `原型文件/[对应项目名]/` 或指定位置 |
| 改进记录 | `文档/改进记录.md` |

---

## 可用技能（.claude/skills/）

### 工作流技能（核心流程，可直接调用）

| 技能 | 做什么 | 何时用 |
|------|--------|--------|
| `orchestrator` | PM 工作流调度中心，串联各技能 | 跑完整需求→PRD→原型→飞书流程 |
| `requirement-exploration` | 模糊想法发散收敛，确定方向 | 还没想清楚要做什么 |
| `requirement-analysis` | 深度分析需求，提问补全细节 | 需求梳理、提炼诉求 |
| `doc-parser` | 拆分原始文档/截图，提取结构信息 | 发来文档/截图要整理 |
| `iteration-planner` | 大需求拆期、MVP、优先级 | 拆分需求、分期、排优先级 |
| `data-analyst` | 报表设计、指标口径定义 | 做报表、定义口径、数据看板 |
| `prd-assistant` | 按模板撰写结构化 PRD | 写 PRD / 生成 PRD |
| `prd-reviewer` | 产品+研发双视角检查 PRD | 检查 PRD / review |
| `prd-review-simulator` | 模拟四角色评审会 | 过评审 / 评审演练 |
| `prd-reviser` | 精准修改已有 PRD | 改 PRD / 修改需求文档 |
| `prd-to-prototype` | PRD 转纯静态 HTML 原型 | 从 PRD 出原型 |
| `competitor-analysis` | 竞品功能拆解，自动搜索补全 | 分析竞品 |
| `drawio-generator` | 生成 .drawio XML 图 | 画流程图/架构图/状态机 |
| `knowledge-base-builder` | 多文档汇总成知识库 | 整理知识库 |
| `feishu-doc` | 创建/写入/编辑飞书云文档 | 发飞书（可选集成） |
| `operation-manual-writer` | 根据截图写操作说明书 | 写操作说明书/用户手册 |

### 模板与参考技能

| 技能 | 做什么 |
|------|--------|
| `prd-template` | PRD 标准模板（通用 + 报表专用），prd-assistant 撰写前必读 |

### 专家角色（由 orchestrator 按需调度，不直接面向用户）

| 技能 | 职责 |
|------|------|
| `biz-expert` | 业务合理性、用户价值、业务规则 |
| `compliance-expert` | 操作日志、权限、金额精度、对账闭环 |
| `finance-expert` | 会计处理、科目、凭证、税务 |
| `ux-expert` | 操作流程、控件选型、状态反馈 |
| `data-expert` | 字段数据来源、计算公式、同步方式 |

### 知识库（按需加载）

| 技能 | 内容 |
|------|------|
| `finance-knowledge` | 通用财务知识（会计/科目/凭证/现金流/税务/业务财务一体化） |

> 💡 业务领域知识通过「知识包」挂载（见 `docs/知识包挂载说明.md`），不在公共包内自带。

---

## 使用方式

### 方式一：通过主持人（推荐）

输入 `/orchestrator`，然后描述需求。主持人会自动识别意图、调度技能、在每个节点衔接。

### 方式二：直接调用单个技能

输入 `/技能名`，例如 `/prd-assistant`、`/requirement-analysis`、`/prd-reviewer`。

### 方式三：自然语言（主持人/模型自动识别）

| 你说的话 | 触发的能力 |
|---------|-----------|
| "我有个需求" | 需求分析 |
| "写 PRD" / "帮我写需求文档" | PRD 撰写 |
| "发飞书" / "发到飞书" | 飞书文档 |
| "改一下" / "修改 PRD" | PRD 修正 |
| "竞品分析" / "看看竞品" | 竞品分析 |
| "画图" / "流程图" | drawio 生成器 |

---

## 主持人核心调度规则（精简版）

完整规则见 `orchestrator` 技能，这里列最核心的几条：

1. **识别优先** — 每次先判断意图，再决定激活哪个技能
2. **计划先行** — 激活前先用 1-3 句话输出执行计划，用户确认后执行（用户说"直接做"可跳过）
3. **节点衔接** — 每个技能完成后输出衔接提示，等用户选择，不自动跳下一步
4. **阶段锁定** — 明确指定的技能优先，锁定当前阶段，达到完成标准且用户确认后才切换
5. **新需求独立处理** — 新开 session 默认视为全新需求，禁止主动检索历史 PRD 关联；只有用户明确说"在 XX 基础上改"时才关联
6. **行动前四问** — 每次生成产物前核对：当前技能是什么？动作是否被允许？前置是否完成？是否授权？

---

## 知识与集成边界

- **未挂载领域知识包时**，不得假设组织规则、术语、字段或接口；检测到领域术语时询问用户是否挂载知识包，无包则向用户澄清。
- **未完成集成配置时**，不得访问、创建、发布或同步外部资源。运行前读取 `config/integration-config.json`（若不存在则视为全部 `disabled`），仅对 `enabled: true` 的集成执行相关动作。
- **涉及外部写入、公开分享、通知或批量更新时**，必须获得用户当前轮次的明确确认。
- **默认本地生成、默认私有、默认不通知。**

---

## 飞书配置（可选集成）

飞书是**可选集成，默认关闭**。启用方式、权限 scope 清单、首次授权步骤见 `docs/飞书集成配置说明.md`。

- 飞书工具封装在 `feishu/` 目录，能力已封装为 MCP Server（`feishu-doc`，见 `.mcp.json`）。
- 首次使用需在 `feishu/` 目录执行 `node feishu_doc.mjs login` 完成授权。
- 机器人通知通过 `feishu/notify.mjs`（读 `.env` 的 `FEISHU_BOT_WEBHOOK`，未配置则静默跳过）。